# minimax-vscode — extracted from this repo (2026-06-10)

The `minimax-vscode/` directory (a standalone VS Code extension for the
MiniMax provider, committed as a bare gitlink without submodule config) was
unrelated to MONITOR's product surface and was removed as part of the
FINAL_FABLE cleanup (T-008). The extension source lived only in the working
tree; if needed, recover it from a filesystem backup or recreate it in its
own repository.
